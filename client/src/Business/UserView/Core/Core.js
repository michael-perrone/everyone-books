import React from 'react';
import styles from './Core.module.css';

const Core = (props) => {
    return (
            <div id={styles.actualCoreContainer}>
                <select>
                    
                </select>
            </div>
    )
}

export default Core;